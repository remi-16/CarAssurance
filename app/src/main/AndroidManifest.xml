<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.carassurance" >

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarAssurance" >

            android:name=".ui.cars.fragments.CarListViewActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.AppActivity" />
        <activity android:name=".ui.settings.SettingActivity" />
        <activity android:name=".ui.settings.AboutUsActivity" />
        <activity android:name=".ui.cars.CarsActivity" />
        <activity android:name=".ui.cars.EditCarActivity" />
        <activity android:name=".ui.incidents.EditIncidentActivity" />
        <activity android:name=".ui.report.ReportActivity" />
        <activity android:name=".ui.incidents.IncidentsActivity" />
    </application>

</manifest>